namespace EchoBase.Core.Entities.Enums;

/// <summary>
/// Indica el lado de la mesa al que pertenece un puesto de trabajo.
/// </summary>
public enum DockSide
{
    /// <summary>Lado A de la mesa (parte frontal o superior según la orientación).</summary>
    A = 0,

    /// <summary>Lado B de la mesa (parte posterior o inferior según la orientación).</summary>
    B = 1
}
